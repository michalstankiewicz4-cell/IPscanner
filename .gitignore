# Local-only secrets - never commit (Blogger API credentials etc.)
.secrets/

node_modules/
package-lock.json
src-tauri/target/
src-tauri/gen/
app/

native-ui/**/bin/
native-ui/**/obj/
release/
release-*/
Pre-Flight Security Audit.txt

# Windows install ISOs (multi-GB, GitHub rejects anything >100MB anyway)
ISO/
*.iso
